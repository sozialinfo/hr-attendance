<?xml version="1.0" encoding="UTF-8" ?>
<templates>
    <t t-name="hr_attendance_kanban.EmployeeInspector" owl="1">
        <div class="o_employee_inspector h-100" t-if="props.employeeId">
            <div class="o_employee_inspector_info">
                <div class="o_overtime_card py-3 text-primary">
                    <strong class="o_overtime_name">Extra Hours</strong>
                    <span class="o_overtime_duration">
                        <t t-esc="formatFloatTime(overtimeState.totalOvertime)" />
                    </span>
                    <div class="text-uppercase">hours</div>
                    <div class="o_overtime_validity text-uppercase">
                        (<span>
                            <t t-if="overtimeState.isCheckedIn">until yesterday</t>
                            <t t-else="">until today</t>
                        </span>)                        
                        
                    </div>
                </div>
                <t t-foreach="state.holidays" t-as="holiday" t-key="holiday[3]">
                    <TimeOffCard
                        name="holiday[0]"
                        data="holiday[1]"
                        requires_allocation="holiday[2] == 'yes'"
                        holidayStatusId="holiday[3]"
                        employeeId="props.employeeId"
                    />
                </t>
                <div class="o_timeoff_card p-0 d-flex justify-content-around">
                    <div
                        class="row justify-content-center align-items-center h-25 w-100 py-3 o_accrual_allocation"
                        t-if="hasAccrualAllocation"
                    >
                        Balance at the
                        <div class="p-1" style="max-width: 100px!important">
                        <DateTimeInput
                            type="'date'"
                            value="state.date"
                            onChange="(date) => this.loadDashboardData(date)"
                            minDate="state.today"
                            placeholder.translate="Today"
                        />
                    </div>
                        <button
                        class="o_timeoff_today_button btn btn-secondary"
                        t-on-click="resetDate"
                    >Today</button>
                    </div>
                    <div
                        class="d-flex flex-column justify-content-center align-items-center w-100 py-3"
                    >
                        <strong class="o_timeoff_name">Pending Requests</strong>
                        <span
                            t-on-click="openPendingRequests"
                            t-att-class="'o_timeoff_duration' + (state.allocationRequests ? ' cursor-pointer' : '')"
                        >
                            <t t-esc="state.allocationRequests" />
                        </span>
                        <a
                            class="text-uppercase o_timeoff_details p-1"
                            t-on-click="newAllocationRequest"
                        >
                            <t t-if="employeeId">Grant Time</t>
                            <t t-else="">New Allocation Request</t>
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </t>
</templates>
